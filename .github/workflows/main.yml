on:
  push:
    branches:
      - master



jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'Testing printing before'
        run: ls
      - uses: actions/checkout@v3  
      - name: 'login to docker hub'
        run: docker login --username fatmaahmed611 --password dckr_pat_N-Km4KvyLRogHo_j6IwLD-OyGPE